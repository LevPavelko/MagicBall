function Answers({respons }){
    return (
        <div style={text}>
            <p style={{color: "white"}}>{respons}</p>
           
        </div>
    );
}
const text ={
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    marginTop: 140
}